bcrypt → Library for secure password hashing.
<!-- Biblioteca para hashing seguro de senhas. -->

black → Automatic Python code formatter for consistent style.
<!-- Formatador automático de código Python para estilo consistente. -->

blinker → Provides signal/event support in Flask.
<!-- Suporte a sinais/eventos dentro do Flask. -->

click → Command-line interface framework (used by Flask).
<!-- Framework para criar comandos de linha de comando (usado pelo Flask). -->

colorama → Cross-platform colored terminal text support.
<!-- Suporte a cores no terminal (principalmente em Windows). -->

flake8 → Linting tool to check code style and errors.
<!-- Ferramenta de linting para verificar estilo e erros no código. -->

Flask → Micro web framework for building ActionHub.
<!-- Microframework web para criar a aplicação ActionHub. -->

Flask-Login → User authentication and session management.
<!-- Gerenciamento de autenticação e sessão de usuários. -->

Flask-SQLAlchemy → ORM integration between Flask and SQLAlchemy.
<!-- ORM para integração entre Flask e SQLAlchemy. -->

Flask-WTF → Flask integration with WTForms for web forms.
<!-- Integração do Flask com WTForms para formulários web. -->

greenlet → Lightweight coroutines, used internally by SQLAlchemy.
<!-- Suporte interno para corrotinas, usado pelo SQLAlchemy. -->

iniconfig → Parser for .ini configuration files (used by pytest).
<!-- Parser de arquivos .ini, usado pelo pytest. -->

itsdangerous → Secure data signing utility (tokens, cookies).
<!-- Utilitário para assinar dados de forma segura (tokens, cookies). -->

Jinja2 → Template engine used by Flask.
<!-- Engine de templates usada pelo Flask. -->

MarkupSafe → Ensures safe string rendering in HTML.
<!-- Garante segurança em strings renderizadas em HTML. -->

marshmallow → Library for data validation and serialization.
<!-- Biblioteca para validação e serialização de dados. -->

marshmallow-sqlalchemy → Auto-generates schemas from SQLAlchemy models.
<!-- Extensão que gera schemas automaticamente a partir das models SQLAlchemy. -->

mccabe → Code complexity checker (used by flake8).
<!-- Verificador de complexidade de código (usado pelo flake8). -->

mypy_extensions → Typing extensions for static type checking.
<!-- Extensões para tipagem estática (mypy). -->

packaging → Utilities for version and dependency handling.
<!-- Utilitário para lidar com versões e dependências. -->

pathspec → Pattern matching for file paths (used by black).
<!-- Usado pelo black para definir padrões de arquivos. -->

platformdirs → Manages platform-specific directories (cache, config).
<!-- Gerencia diretórios específicos da plataforma (cache, config). -->

pluggy → Plugin system used by pytest.
<!-- Sistema de plugins usado pelo pytest. -->

pycodestyle → PEP8 style checker (used by flake8).
<!-- Verificador de estilo PEP8 (usado pelo flake8). -->

pyflakes → Error checker for Python code (used by flake8).
<!-- Verificador de erros de código (usado pelo flake8). -->

pytest → Python testing framework.
<!-- Framework de testes para Python. -->

python-dotenv → Loads environment variables from .env file.
<!-- Carrega variáveis de ambiente do arquivo .env. -->

SQLAlchemy → ORM for database management.
<!-- ORM para manipulação do banco de dados. -->

typing_extensions → Extra typing features for older Python versions.
<!-- Recursos extras de tipagem para versões antigas do Python. -->

Werkzeug → WSGI toolkit used by Flask for requests/responses.
<!-- Toolkit WSGI usado pelo Flask para requisições/respostas. -->

WTForms